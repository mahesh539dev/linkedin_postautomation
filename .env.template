# Claude API — console.anthropic.com → API Keys → Create Key
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Buffer — buffer.com/developers/apps → Create App → copy Access Token
BUFFER_ACCESS_TOKEN=your-buffer-access-token
# buffer.com/manage → click LinkedIn channel → copy ID from URL
BUFFER_PROFILE_ID=your-linkedin-channel-id

# Security — any random string you make up
APPROVAL_SECRET=mahesh2025linkedin99

# Gmail
SMTP_EMAIL=mahesh.annapureddy5@gmail.com
# myaccount.google.com → Security → App passwords (NOT Gmail password)
SMTP_PASSWORD=your-16-char-app-password
NOTIFY_EMAIL=mahesh.annapureddy5@gmail.com

# Updated after Railway deploy
BASE_URL=https://your-app.up.railway.app
FLASK_ENV=production

# Your Week 1 start date (Monday of week 1)
JOURNEY_START_DATE=2025-01-06

# OpenRouter — openrouter.ai/keys → Create Key → add ~$5 credit (~200 runs)
# Used for DeepSeek topic ranking + Kimi post generation (~$0.014/run)
OPENROUTER_API_KEY=sk-or-your-key-here

# OpenAI — platform.openai.com/api-keys → Create key
# Used for GPT-4o V3 rewrites + scoring all 3 versions (~$0.035/run)
OPENAI_API_KEY=sk-your-openai-key-here

# SerpAPI (OPTIONAL) — serpapi.com → Dashboard → API Key
# Free tier: 100 searches/month (covers 4 weekly runs). Leave blank for HN-only.
SERPAPI_KEY=

# Set to true to activate the multi-model pipeline (HN + DeepSeek + Kimi + Haiku)
# false = use curated fallback topics (no API calls for research)
USE_WEB_RESEARCH=true
